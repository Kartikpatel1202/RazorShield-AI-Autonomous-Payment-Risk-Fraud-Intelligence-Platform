# RazorShield AI - frontend

React 19 + TypeScript + Vite application shell.

Setup, environment variables and Docker instructions live in the
[repository README](../README.md).

```
npm install
npm run dev      # http://localhost:5173/dashboard
npm test
npm run lint
npm run build
```
