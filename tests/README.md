# tests/

Cross-cutting, end-to-end tests that span more than one package.

Phase 1 has none: backend foundation tests live in `backend/tests/` and frontend
tests live beside their components in `frontend/src/`.
